import DeleteButton from './DeleteButton';

export default {
  title: 'Components/Atoms/DeleteButton',
  component: DeleteButton,
};

const Template = (args) => <DeleteButton {...args}></DeleteButton>;

export const Default = Template.bind({});
